services:
  migrations:
    build:
      context: .
      dockerfile: docker/migrations.Dockerfile
    image: academico/migrations:local
    entrypoint: ["sh"]
    command: ["/usr/local/bin/apply-migrations.sh"]
